with (global._net){
    if (_map[?"username"] != undefined) username = _map[?"username"];
    if (_map[?"lasturl"] != undefined) url = _map[?"lasturl"];
    if (_map[?"lastport"] != undefined) port = _map[?"lastport"];
}